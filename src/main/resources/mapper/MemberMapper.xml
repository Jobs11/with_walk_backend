<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.with_walk.mapper.MemberMapper">

	<insert id="registerUser" parameterType="com.example.with_walk.dto.MemberVO">
		insert into
			member (m_id, m_password, m_name, m_nickname, m_email, m_paint)
		values
			(#{m_id}, #{m_password},#{m_name}, #{m_nickname}, #{m_email},  #{m_paint})
	</insert>

	<select id="getUser">
		select * from member where m_id = #{m_id} and m_password = #{m_password}
	</select>

	<select id="getUserData">
		select * from member where m_id = #{m_id}
	</select>

    <update id="updateUser">
		update
			member
		set
			m_password=#{m_password}, m_nickname=#{m_nickname}, m_name=#{m_name}, m_email=#{m_email}, m_paint = #{m_paint}
		where
			m_id = #{m_id}
	</update>

    <delete id="deleteUser">
		delete from member where m_id = #{m_id}
	</delete>

	<update id="updateProfile">
		update
			member
		set
			m_paint = #{m_paint}
		where
			m_id = #{m_id}
	</update>


</mapper>
